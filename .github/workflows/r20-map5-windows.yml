name: R20 Scientific HD MAP5 Windows x64

on:
  push:
    branches:
      - r20-scientific-hd
    paths:
      - '.github/workflows/r20-map5-windows.yml'
      - 'tools/r20_src.b64'
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Reconstruct exact packaged R20 source
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force engine\thirdparty, bin | Out-Null
          $b64=(Get-Content tools\r20_src.b64 -Raw).Trim()
          [IO.File]::WriteAllBytes("r20_src.gz",[Convert]::FromBase64String($b64))
          python -c "import gzip,pathlib; pathlib.Path(r'engine/hilal_unified_map5_engine.cc').write_bytes(gzip.decompress(pathlib.Path('r20_src.gz').read_bytes()))"
          $src=(Get-FileHash engine\hilal_unified_map5_engine.cc -Algorithm SHA256).Hash.ToLowerInvariant()
          if ($src -ne "43f275c5187a31ea1acaf14432aad51032c170893aa74a1a2f91e697f60fcb58") { throw "R20 source SHA mismatch: $src" }

      - name: Fetch exact frozen Astronomy Engine dependencies
        shell: pwsh
        run: |
          function Fetch-GitBlob([string]$Sha,[string]$OutFile) {
            $u="https://api.github.com/repos/cosinekitty/astronomy/git/blobs/"+$Sha
            $j=Invoke-RestMethod -Uri $u -Headers @{ "User-Agent"="ALBAZ-R20-HD" }
            $b64=([string]$j.content) -replace "\s",""
            [IO.File]::WriteAllBytes($OutFile,[Convert]::FromBase64String($b64))
          }
          Fetch-GitBlob "60d36e3002be6e58ad75619649bff4aabcc43239" "engine\thirdparty\astronomy.c"
          Fetch-GitBlob "9d2455980102eda0c0fda6e66ac9fefd0c999712" "engine\thirdparty\astronomy.h"
          Invoke-WebRequest -Uri "https://raw.githubusercontent.com/nothings/stb/master/stb_image_write.h" -OutFile "engine\thirdparty\stb_image_write.h"
          if ((Get-FileHash engine\thirdparty\astronomy.c -Algorithm SHA256).Hash.ToLowerInvariant() -ne "a7f6bde24e3a53f24aff727080916809b5664117ebf3fef6e916da56a51d4fbd") { throw "astronomy.c SHA mismatch" }
          if ((Get-FileHash engine\thirdparty\astronomy.h -Algorithm SHA256).Hash.ToLowerInvariant() -ne "d9cc0379035b69f1b75034879398e254036c708448d5427396596cd2c7f0d843") { throw "astronomy.h SHA mismatch" }

      - name: Build exact R20 PPD1 and PPD4 engines
        shell: pwsh
        run: |
          $vswhere="${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          $vs=& $vswhere -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath
          if (-not $vs) { throw "Visual Studio C++ toolchain not found" }
          $vcvars=Join-Path $vs "VC\Auxiliary\Build\vcvars64.bat"
          $cmd=@"
          @echo off
          call "$vcvars"
          if errorlevel 1 exit /b 1
          cl /nologo /O2 /EHsc /std:c++20 /openmp /DNDEBUG /DPIXEL_PER_DEGREE=1 engine\hilal_unified_map5_engine.cc engine\thirdparty\astronomy.c /Fe:bin\hilal_unified_map5_engine_fast.exe
          if errorlevel 1 exit /b 1
          cl /nologo /O2 /EHsc /std:c++20 /openmp /DNDEBUG /DPIXEL_PER_DEGREE=4 engine\hilal_unified_map5_engine.cc engine\thirdparty\astronomy.c /Fe:bin\hilal_unified_map5_engine.exe
          if errorlevel 1 exit /b 1
          "@
          Set-Content build_r20.cmd $cmd -Encoding ASCII
          cmd /c build_r20.cmd
          if ($LASTEXITCODE -ne 0) { throw "MSVC build failed" }

      - name: Verify R20 signatures and ALBAZ boundary
        shell: pwsh
        run: |
          $v1=& .\bin\hilal_unified_map5_engine_fast.exe version | Out-String
          $v4=& .\bin\hilal_unified_map5_engine.exe version | Out-String
          Write-Host $v1
          Write-Host $v4
          foreach ($pair in @(@($v1,"ppd=1"),@($v4,"ppd=4"))) {
            if ($pair[0] -notmatch "ALBAZ_UNIFIED_MAP5_ENGINE_R20_HD" -or $pair[0] -notmatch "batch=map5" -or $pair[0] -notmatch "render=clean-categorical-full-grid" -or $pair[0] -notmatch [regex]::Escape($pair[1])) { throw "R20 signature mismatch" }
          }
          $b=& .\bin\hilal_unified_map5_engine_fast.exe albaz-eval 1 6.009854500758429 | Out-String
          Write-Host $b
          if ($b -notmatch "V=([-+0-9.eE]+)") { throw "V missing" }
          if ([Math]::Abs([double]$Matches[1]) -gt 1e-12) { throw "V boundary mismatch" }
          if ($b -notmatch "P=([-+0-9.eE]+)") { throw "P missing" }
          if ([Math]::Abs([double]$Matches[1]-0.376742538335999) -gt 1e-12) { throw "P boundary mismatch" }

      - name: Smoke PPD1 five-map render
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force smoke1 | Out-Null
          $env:OMP_NUM_THREADS="4"
          $sw=[Diagnostics.Stopwatch]::StartNew()
          & .\bin\hilal_unified_map5_engine_fast.exe 2026-09-12 map5 evening smoke1\odeh.png smoke1\yallop.png smoke1\allawi.png smoke1\saao.png smoke1\albaz.png
          if ($LASTEXITCODE -ne 0) { throw "PPD1 smoke failed" }
          $sw.Stop()
          Write-Host ("R20_PPD1_SECONDS="+[Math]::Round($sw.Elapsed.TotalSeconds,3))
          foreach ($n in @("odeh","yallop","allawi","saao","albaz")) { if (!(Test-Path "smoke1\$n.png")) { throw "missing smoke1 $n" } }

      - name: Smoke PPD4 five-map final render
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force smoke4 | Out-Null
          $env:OMP_NUM_THREADS="4"
          $sw=[Diagnostics.Stopwatch]::StartNew()
          & .\bin\hilal_unified_map5_engine.exe 2026-09-12 map5 evening smoke4\odeh.png smoke4\yallop.png smoke4\allawi.png smoke4\saao.png smoke4\albaz.png
          if ($LASTEXITCODE -ne 0) { throw "PPD4 smoke failed" }
          $sw.Stop()
          Write-Host ("R20_PPD4_SECONDS="+[Math]::Round($sw.Elapsed.TotalSeconds,3))
          Add-Type -AssemblyName System.Drawing
          $im=[System.Drawing.Image]::FromFile((Resolve-Path smoke4\albaz.png))
          try { if ($im.Width -ne 1440 -or $im.Height -ne 720) { throw "PPD4 dimensions mismatch" } } finally { $im.Dispose() }

      - name: Hash exact R20 engines
        shell: pwsh
        run: |
          Get-FileHash bin\hilal_unified_map5_engine_fast.exe -Algorithm SHA256 | Format-List | Out-File bin\SHA256_R20_MAP5.txt
          Get-FileHash bin\hilal_unified_map5_engine.exe -Algorithm SHA256 | Format-List | Out-File -Append bin\SHA256_R20_MAP5.txt
          Get-Content bin\SHA256_R20_MAP5.txt

      - name: Upload exact R20 Windows engines
        uses: actions/upload-artifact@v4
        with:
          name: ALBAZ_R20_MAP5_WINDOWS_X64_EXACT
          path: |
            bin/hilal_unified_map5_engine_fast.exe
            bin/hilal_unified_map5_engine.exe
            bin/SHA256_R20_MAP5.txt
            smoke1/*.png
            smoke4/*.png
          if-no-files-found: error
